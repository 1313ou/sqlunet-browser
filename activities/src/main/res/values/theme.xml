<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright (c) 2023. Bernard Bou
  -->

<resources>

	<style name="MyTheme" parent="Theme.Material3.DayNight.NoActionBar">

		<!-- C O L O R S -->

		<!-- framework colors -->
		<item name="colorSurface">@color/primaryColor</item>
		<item name="colorPrimary">@color/primaryColor</item>
		<item name="colorOnSurface">@color/onPrimaryColor</item>
		<item name="colorOnPrimary">@color/onPrimaryColor</item>-->

		<!-- nav bar -->
		<item name="colorPrimaryVariant">@color/primaryLightColor</item>
		<item name="colorPrimaryDark">@color/primaryDarkColor</item>-->

		<!-- requires attention : snackbar, fab -->
		<item name="colorSecondary">@color/secondaryColor</item>-->
		<item name="colorSecondaryVariant">@color/secondaryLightColor</item>-->
		<item name="colorOnSecondary">@color/onSecondaryColor</item>-->

		<!-- background of activated indicator, caret -->
		<item name="colorControlActivated">@color/secondaryColor</item>

		<!-- O V E R R I D E S -->

		<!-- transparent statusbar and navigation bar -->
		<item name="android:statusBarColor">@android:color/transparent</item>
		<item name="android:navigationBarColor">@android:color/transparent</item>

		<!-- whether this Window is responsible for drawing the background for the system bars.
		     if true and the window is not floating, the system bars are drawn with a
             transparent background and the corresponding areas in this window are filled with the
             colors specified in android.R.attr#statusBarColor and android.R.attr#navigationBarColor-->
		<item name="android:windowDrawsSystemBarBackgrounds">true</item>

		<!-- Force drawing behind system bars -->
		<!-- whether this window requests a translucent status bar -->
		<item name="android:windowTranslucentStatus">false</item>
		<!-- whether this window requests a translucent navigation bar -->
		<item name="android:windowTranslucentNavigation">false</item>

		<!-- Remove contrast enforcement (API 29+) -->
		<!-- whether the system should ensure that the status bar has enough contrast when a fully transparent background is requested.
             if true, the system will determine whether a scrim is necessary to ensure that the status bar has enough contrast with the contents of
             this app, and set an appropriate effective bar background color accordingly. -->
		<item name="android:enforceStatusBarContrast">false</item>
		<!-- whether the system should ensure that the navigation bar has enough contrast when a fully transparent background is requested.
             if true, the system will determine whether a scrim is necessary to ensure that the navigation bar has enough contrast with the contents of
             this app, and set an appropriate effective bar background color accordingly. -->
		<item name="android:enforceNavigationBarContrast">false</item>

		<!-- S T Y L E S -->

		<!-- menu dropdown -->
		<item name="popupMenuBackground">@color/primaryColor</item>

	</style>

</resources>
