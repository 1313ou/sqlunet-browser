package com.bbou.download

/**
 * Keys
 */
object Keys {

    // A R G U M E N T   K E Y S

    /**
     * Download mode argument
     */
    const val DOWNLOAD_MODE_ARG = "download_mode"

    /**
     * From argument
     */
    const val DOWNLOAD_FROM_ARG = "download_from"

    /**
     * Rename from argument
     */
    const val RENAME_FROM_ARG = "rename_from"

    /**
     * Rename to argument
     */
    const val RENAME_TO_ARG = "rename_to"

    /**
     * Unzip to argument
     */
    const val THEN_UNZIP_TO_ARG = "unzip_to"

    /**
     * Rename from argument
     */
    const val DOWNLOAD_RENAME_FROM_ARG = "rename_from"

    /**
     * Rename to argument
     */
    const val DOWNLOAD_RENAME_TO_ARG = "rename_to"

    /**
     * Target file, the file to download or extracted and deployed from it
     */
    const val DOWNLOAD_TARGET_FILE_ARG = "target_file"

    // plain

    /**
     * To argument
     */
    const val DOWNLOAD_TO_FILE_ARG = "download_to_file"

    // zip

    /**
     * To argument
     */
    const val DOWNLOAD_TO_DIR_ARG = "download_to_dir"

    /**
     * Zip entry argument
     */
    const val DOWNLOAD_ENTRY_ARG = "entry"

    // B R O A C A S T   K E Y S

    /**
     * Broadcast action
     */
    const val BROADCAST_ACTION = "ACTION"

    /**
     * Broadcast request key
     */
    const val BROADCAST_REQUEST_KEY = "REQUEST"

    /**
     * Broadcast kill (old datapack) request value
     */
    const val BROADCAST_KILL_REQUEST_VALUE = "KILL"

    /**
     * Broadcast new (datapack) request value
     */
    const val BROADCAST_NEW_REQUEST_VALUE = "NEW"

    // S T A T E   K E Y S

    /**
     * Instance state key for download button
     */
    const val DOWNLOAD_BTN_STATE = "download_btn_state"

    /**
     * Instance state key for download button image resource id
     */
    const val DOWNLOAD_BTN_IMAGE_STATE = "download_image_btn_state"

    /**
     * Instance state key for download button background resource id
     */
    const val DOWNLOAD_BTN_BACKGROUND_STATE = "download_background_btn_state"

    /**
     * Instance state key for cancel button
     */
    const val CANCEL_BTN_STATE = "cancel_btn_state"

    /**
     * Instance state key for deploy button
     */
    const val DEPLOY_BTN_STATE = "deploy_btn_state"

    /**
     * Instance state key for md5 button
     */
    const val MD5_BTN_STATE = "md5_btn_state"

    /**
     * Instance state key for progress
     */
    const val PROGRESS_STATE = "progress_state"

    /**
     * Instance state key for progress status text view
     */
    const val PROGRESS_STATUS_STATE = "progress_status_state"
}